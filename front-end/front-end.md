### Front-end server doc

#### How to install:

Make sure you have node.js installed.

Under `/front-end`: `npm install`

Then `npm start`. 

Then you should be able to see the result at `localhost:3000`. 

